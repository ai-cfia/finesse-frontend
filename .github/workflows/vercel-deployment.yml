name: Vercel Preview Deployment

on:
  push:
  pull_request:

jobs:

  vercel-deployment-dev:
    uses: ai-cfia/github-workflows/.github/workflows/workflow-vercel-deployment.yml@58-allow-the-use-of-env-file-to-frontend-static-deployment-workflows
    with:
        project-name: 'finesse-frontend'
        deployment-environment: 'dev'
    secrets: inherit

  vercel-deployment-uat:
    if: github.ref == 'refs/heads/main'
    uses: ai-cfia/github-workflows/.github/workflows/workflow-vercel-deployment.yml@58-allow-the-use-of-env-file-to-frontend-static-deployment-workflows
    with:
        project-name: 'finesse-frontend'
        deployment-environment: 'uat'
    secrets: inherit
