name: Build and Deploy to GitHub Pages

on:
  push:
    branches:
      - testing-branch-gh-pages-deployment

jobs:
  build-and-deploy:
    uses: ai-cfia/github-workflows/.github/workflows/workflow-gh-pages-deployment.yml@temporary-branch-gh-pages-deployment
    secrets: { secrets.ENV }
